const suggestions = [{ text: 'Can I patent and sell a classical churna in India?', type: 'classical', tag: 'Traditional knowledge' }, { text: 'What obligations apply if I use a medicinal plant for research in India?', type: 'plant-research', tag: 'Biodiversity' }, { text: 'What should I consider if I want to sell my product abroad?', type: 'export', tag: 'Global market' }];
export default function SuggestedQuestions({ onSelect }) { return <section className="suggestions" aria-label="Suggested questions">{suggestions.map((item, index) => <button key={item.type} onClick={() => onSelect(item)} className="suggestion-card"><span>0{index + 1} · {item.tag}</span><strong>{item.text}</strong><i>↗</i></button>)}</section>; }
